build:
  image: golang:1.5
  commands:
    - make deps
    - make build
    - go test

publish:
  docker:
    user: $$DOCKER_USER
    email: $$DOCKER_EMAIL
    password: $$DOCKER_PASS
    repo: mikkeloscar/drone-pkgbuild
    tag: latest
    when:
      branch: master
